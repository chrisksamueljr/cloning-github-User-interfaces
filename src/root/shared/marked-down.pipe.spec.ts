import { MarkedDownPipe } from './marked-down.pipe';

describe('MarkedDownPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkedDownPipe();
    expect(pipe).toBeTruthy();
  });
});
